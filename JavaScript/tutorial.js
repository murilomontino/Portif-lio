function maior(x, y) {
    document.getElementById('Resultado').innerText = x > y ? x : y
}

function VerificarEntrada(convidado, label){
    const Convidados = ['Monyse', 'Murilo', 'Matheus']
    console.log(convidado)
    label.innerText = "VAGABUNDO!"
}

